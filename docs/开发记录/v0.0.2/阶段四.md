# 阶段四开发
## 任务拆解
- 任务 1：实现分享直开页路由
  - 新增 `/share/[uuid]` 页面
  - 页面加载即调用 `/poc/{uuid}` 并渲染 Sandpack
- 任务 2：主页面接入分享页入口
  - 当存在 `share_uuid` 时提供“打开分享页”链接
- 任务 3：合同结构化报价增强
  - 从 PRD 文本抽取报价字段（单价、数量、税率、总价）
  - 生成 `quote_payload` 并注入合同正文
  - 合同记录持久化 `quote_payload`
- 任务 4：端到端验证
  - 验证 `/poc/{share_uuid}` 可读取
  - 验证合同生成与下载可用
## 实现功能与代码逻辑
- 已实现阶段四全部任务（P2-体验增强）
- 分享直开页已完成
  - 新增路由页：`/share/[uuid]`
  - 页面自动请求 `/poc/{uuid}` 并渲染代码预览
  - 代码位置：`frontend/src/app/share/[uuid]/page.tsx`
- 主页面分享入口已接入
  - 在生成 POC 后显示“打开分享页”链接
  - 代码位置：`frontend/src/app/page.tsx`
- 合同结构化报价增强已完成
  - 新增 `extract_quote_payload`：从 PRD 文本提取报价结构
  - 新增 `format_quote_payload`：格式化报价字段写入合同正文
  - `generate_contract` 增强：持久化 `quote_payload` 并输出增强合同
  - 代码位置：`backend/app/main.py`
- 阶段四验证结果（节选）
  - `poc ... {"status_code": 200, "fetch_status_code": 200}`
  - `contract ... {"status_code": 200, "download_status_code": 200}`
