# 阶段三开发
## 任务拆解
- 任务 1：实现核心实体持久化落盘能力
  - 统一状态存储文件 `backend/storage/runtime_state.json`
  - 覆盖 sessions/utterances/prds/pocs/contracts/voice_profiles/knowledge/prd_citations
- 任务 2：实现服务启动恢复
  - 启动时自动加载 runtime_state
  - 向量字段恢复为 `numpy.ndarray` 以保持检索可用
- 任务 3：切换关键读写链路到持久化路径
  - 逐字稿写入、PRD 保存、POC 生成、合同生成、知识库上传/重建/索引后均触发持久化
  - `/poc/{share_uuid}` 与 `/contract/{contract_id}/download` 保持兼容读取
- 任务 4：重启可恢复能力验证
  - 验证 share_uuid 映射落盘
  - 验证合同映射落盘
## 实现功能与代码逻辑
- 已实现阶段三全部任务（P1-真实落库）
- 持久化基础设施已完成
  - 新增 `get_storage_root_dir`、`get_runtime_state_path`
  - 新增 `save_runtime_state`、`load_runtime_state`
  - 新增序列化/反序列化逻辑：`serialize_voice_embeddings`、`serialize_knowledge_chunks`
  - 代码位置：`backend/app/main.py`
- 启动恢复机制已接入
  - 应用初始化阶段执行 `load_runtime_state()`
  - 自动恢复核心缓存与向量字段
  - 代码位置：`backend/app/main.py`
- 关键读写链路已切换到持久化
  - `voice_register`、`transcribe`、`session_summary`、`save_prd`、`generate_poc`、`generate_contract`
  - `upload_knowledge`、`reindex_knowledge_document`、`index_knowledge_document`
  - 代码位置：`backend/app/main.py`
- 阶段三验证结果（节选）
  - `persistence ... {"runtime_exists": true, "poc_mapping_persisted": true, "contract_count": 1}`
